default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
development:
  <<: *default
  host: localhost
  username: pic_now
  password:
  database: pic_now_development
test:
  <<: *default
  host: localhost
  username: pic_now
  password:
  database: pic_now_test
production:
  <<: *default
  database: pic_now_production
  username: pic_now
  host: 10.41.187.167
  password: <%= ENV['PIC_NOW_DATABASE_PASSWORD'] %>
